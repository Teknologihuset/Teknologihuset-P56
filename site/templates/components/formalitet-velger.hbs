{{#highlight-button
    classNames="tn-btn-left"
    selectedClassName=selectedClassName
    nonSelectedClassName=nonSelectedClassName
    selected=controller.isFormelt
    active=controller.formalitetValgt
    clickedAction="velgFormalitet"}}

    Formelt
{{/highlight-button}}

{{#highlight-button
    classNames="tn-btn-right"
    selectedClassName=selectedClassName
    nonSelectedClassName=nonSelectedClassName
    selected=controller.isUformelt
    active=controller.formalitetValgt
    clickedAction="velgUformalitet"}}

    UFormelt
{{/highlight-button}}